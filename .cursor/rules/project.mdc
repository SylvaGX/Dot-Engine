---
description: Project rules
globs: always
---

# Project rules

- When creating plans if a lib is needed, try to check if it is in the conan repo. If it is, say it with a warning to add it, since the user has the conan extension installed. If it doesn't, ask the user to install it under the vendor folder.
- Ensures that when reading files, it avoids the TOCTOU vulnerability. So it needs to do atomic operations. This could mean opening a file in appended mode, or reading and writing in a single operation. And then validating the file contents, so we know it is a trusted file.

## File naming convention (DOD C++)

Every header must fit exactly one of these five categories:

- `*Types.h` — data only: structs, enums, POD handles. No methods. Namespace = module root (`DotEngine`, `DotData`).
- `*System.h` / `*System.cpp` — free functions that operate on types. Namespace must be `DotEngine::<FileStem>` (e.g. `BufferSystem.h` → `namespace DotEngine::Renderer`).
- `*Context.h` — one aggregate state struct. File name must equal type name. No other types.
- `*Macros.h` — preprocessor defines only. No types, no namespaces.
- `<ClassName>.h` / `<ClassName>.cpp` — one class per file, file name equals class name.

Rules:
- A `*Types.h` file may group multiple related POD structs (e.g. `RenderTypes.h`, `WindowTypes.h`).
- A `*System.h` file must not define types; forward-declare them from the matching `*Types.h`.
- Never mix categories in a single file.

## Structure

```text
DotEngine
    |
    +-- DotInfrastructure
    |   |
    |   +-- DotFilesystem (Virtual filesystem, IO abstraction, hot-reload system)
    |   |   |
    |   |   +-- File loading / streaming
    |   |   +-- Directory watching (hot reload assets)
    |   |   +-- Path resolution (mount points, virtual paths)
    |   |   +-- Async file IO system
    |   |
    |   +-- DotSerialization (Binary / text / custom formats)
    |   |   |
    |   |   +-- Object serialization (scene, assets, ECS snapshot)
    |   |   +-- Versioning system (schema migration)
    |   |   +-- Reflection-based serialization
    |   |   +-- Stream-based serializers
    |   |
    |   +-- DotReflection (Runtime type system, metadata, RTTI replacement)
    |   |   |
    |   |   +-- Type registration system
    |   |   +-- Property inspection
    |   |   +-- Function metadata (editor binding)
    |   |   +-- Enum / struct introspection
    |   |
    |   +-- DotSchema (Validation rules for engine data)
    |   |   |
    |   |   +-- Structural validation
    |   |   +-- Range / constraint checks
    |   |   +-- Asset validation rules
    |   |
    |   +-- DotMath (Core math library)
    |   |   |
    |   |   +-- Vector / Matrix / Quaternion types
    |   |   +-- Transform utilities
    |   |   +-- SIMD optimizations
    |   |
    |   +-- DotMemory (Memory management system)
    |   |   |
    |   |   +-- Arena allocators
    |   |   +-- Pool allocators
    |   |   +-- Frame allocators
    |   |   +-- Memory tracking / leak detection
    |   |
    |   +-- DotLogging (Debug & runtime logging system)
    |       |
    |       +-- Log channels (Engine / Game / Editor)
    |       +-- Log levels (Info / Warning / Error / Critical)
    |       +-- Console / file logging
    |       +-- Assertion system
    |
    +-- DotEngineData
    |   |
    |   +-- DotAssets (Asset system + import pipeline)
    |   |   |
    |   |   +-- Asset database (GUID system)
    |   |   +-- Import pipeline (converters, processors)
    |   |   +-- Dependency tracking
    |   |   +-- Streaming system
    |   |   |
    |   |   +-- Asset types
    |   |       |
    |   |       +-- Shader source files (.hlsl, .msl, .glsl)
    |   |       +-- Texture source files (.ktx, .dds, .png, .jpg)
    |   |       +-- Mesh files (.fbx, .gltf, .obj)
    |   |       +-- Audio files (.wav, .mp3, .ogg)
    |   |       +-- Material definitions (.mat)
    |   |       +-- Scene files (.scene)
    |   |       +-- Animation files (.anim)
    |   |       +-- Script files (.lua, .cs, .js)
    |   |
    |   +-- DotConfig ("I configure the engine itself")
    |   |   |
    |   |   +-- Engine settings (graphics, physics, audio)
    |   |   +-- Project settings
    |   |   +-- Feature toggles
    |   |   +-- Platform profiles
    |   |
    |   +-- DotScene (World representation, hierarchy, entities)
    |   |   |
    |   |   +-- Scene graph / hierarchy system
    |   |   +-- Prefab system
    |   |   +-- Scene serialization
    |   |
    |   +-- DotECS (DATA ONLY LAYER)
    |   |   |
    |   |   +-- ECS Storage (Archetypes / SoA tables)
    |   |   +-- Component definitions (pure data)
    |   |   +-- Entity registry
    |   |   +-- Query system (archetype filtering)
    |   |
    |   +-- DotPhysicsData
    |   |   |
    |   |   +-- Collision shapes
    |   |   +-- Rigid body definitions
    |   |   +-- Physics materials
    |   |
    |   +-- DotAnimationData
    |       |
    |       +-- Skeletons / rigs
    |       +-- Animation clips
    |       +-- Blend trees
    |
    +-- DotRuntime
    |   |
    |   +-- DotGraphics (RHI - Rendering Hardware Interface)
    |   |   |
    |   |   +-- Vulkan backend
    |   |   +-- DirectX12 backend
    |   |   +-- Metal backend
    |   |
    |   +-- DotRenderer
    |   |   |
    |   |   +-- Frame graph
    |   |   +-- Scene rendering
    |   |   +-- Culling / batching
    |   |   +-- Lighting / post-processing
    |   |
    |   +-- DotPhysics (Runtime simulation)
    |   |   |
    |   |   +-- Rigid body simulation
    |   |   +-- Collision detection
    |   |   +-- Constraint solver
    |   |
    |   +-- DotAudio
    |   |   |
    |   |   +-- Spatial audio
    |   |   +-- Mixing / buses
    |   |   +-- DSP effects
    |   |
    |   +-- DotInput
    |   |   |
    |   |   +-- Keyboard / mouse / controller input
    |   |   +-- Input mapping system
    |   |   +-- Action bindings
    |   |
    |   +-- DotNetworking
    |   |   |
    |   |   +-- Client/server model
    |   |   +-- RPC system
    |   |   +-- Replication system
    |   |
    |   +-- DotScripting (Runtime scripting system)
    |   |   |
    |   |   +-- Lua / C# / custom VM integration
    |   |   +-- Script lifecycle (init / update / destroy)
    |   |   +-- Binding to ECS + engine API
    |   |
    |   +-- DotUI (Immediate + retained UI system)
    |   |   |
    |   |   +-- Runtime UI rendering system
    |   |   +-- Widgets (buttons, panels, text, images)
    |   |   +-- Layout system
    |   |   +-- Input-driven UI interaction
    |   |
    |   +-- Runtime Core
    |       |
    |       +-- Application Loop
    |       +-- ECS Execution
    |       +-- Frame orchestration
    |       +-- Job system (multithreading)
    |
    +-- DotEditor
    |   |
    |   +-- Scene editing framework
    |   +-- Inspector system (reflection-based UI)
    |   +-- Scene Editor (gizmos, transforms, hierarchy)
    |   +-- Asset Browser backend
    |   |   |
    |   |   +-- Asset database queries (GUID → asset)
    |   |   +-- Dependency graph lookup
    |   |   +-- Import status (dirty, missing, cached)
    |   |   +-- Thumbnail generation requests
    |   |   +-- Filtering / search indexing
    |   |   +-- Virtual folder mapping (if you have one)
    |   |   +-- Asset metadata handling
    |   |
    |   +-- Undo/redo
    |   +-- Debug Tools (profiler, frame debugger)
    |   +-- Editor UI framework (built with ImGui)
    |   +-- Play Mode (runs DotRuntime inside editor)
    |   
    +-- DotTools
    |    |
    |    +-- DotBuild
    |    +-- DotAssetCooker
    |    +-- DotShaderCompiler
    |
    +-- DotApps
        |
        +-- Games (final shipped builds)
        |
        +-- Tools Applications
        |   |
        |   +-- Asset Compiler / Cooker
        |   +-- Shader Compiler
        |   +-- Scene Converter tools
        |
        +-- DotEditorApp
        |   |
        |   +-- Startup
        |   +-- Window creation
        |   +-- Editor UI
        |   |   |
        |   |   +-- Asset Browser UI
        |   |   +-- Scene Editor UI
        |   |   +-- Inspector UI
        |   |   +-- Debug Tools UI
        |   |   +-- Play Mode UI
        |   |   +-- Profiler UI
        |   |   +-- Frame Debugger UI
        |   |   +-- Console UI
        |   |   +-- Editor UI (ImGui)
        |   |
        |   +-- Loading DotEditor layer
        |   +-- Runtime loop wiring
        |
        +-- Sandbox Applications
            |
            +-- Experimental builds
            +-- Feature prototyping
```

## Pure DOD principles

Default contract (unless a plan explicitly opts out):

- Data in `*Types.h` / context structs — POD only, no methods, no virtuals, no I/O
- Behavior in `*System.h` — free functions; pass explicit slices (`EngineContext&`, `EditorContext&`, `GameInputState&`)
- No thin getters that only return a field — read `ctx.field` directly
- No hidden module globals — state on context structs; Application wires slices
- Backend systems write context slices at init; consumers read fields directly
- No virtual dispatch in the frame loop
- SoA for 100+ homogeneous hot-path items; singleton subsystems stay AoS

Architecture PRs must include a **DOD compliance** note: POD vs systems added, opt-outs, and runtime vs editor layer touched.
